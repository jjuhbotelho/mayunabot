{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executa Bot",
            "type": "php",
            "request": "launch",
            "program": "${workspaceFolder}/bot.php",
            "cwd": "${workspaceFolder}",
            "port": 9000,
            "env": {
                "XDEBUG_CONFIG": "remote_enable=1 remote_autostart=1 remote_mode=req remote_connect_back=0 remote_host=localhost remote_port=9000"
            }
        }
    ]
}
